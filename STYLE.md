You must follow this style when you write code for this project:

* do not add any comments.
* prefer compact but readable code
* use short imports, e.g. from os import path
* do not call APIs directly, extract when either into helper functions or classes
